;; Template — copy to resources/secrets.edn (or project-root secrets.edn).

{:users
 {:admin {:password "admin"
          :roles #{:admin}
          :email ["admin@example.com"]}
  :trader {:password "trader"
           :roles #{:trader}
           :email ["trader@example.com"]}
  :viewer {:password "viewer"
           :roles #{:viewer}
           :email ["viewer@example.com"]}}

 :token
 {:secret "change-me-in-production"
  :oauth2 {:google {:client-id ""
                    :client-secret ""}
           :github {:client-id ""
                    :client-secret ""}}}}
